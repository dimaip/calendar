import MdxLoader from 'containers/Service/Texts/MdxLoader';
import Parts from 'components/Parts/Parts';

import Exapostilari from 'containers/Service/Texts/Matins/Exapostilari/Exapostilari';
import Psalm50 from 'containers/Service/Texts/Shared/Psalm50/index.mdx';

<span id="nachalo" />

# Утреня

<br />

**П** Слава святой, и единосущной, и животворящей, и нераздельной Троице ныне и всегда и во веки веков!

**Н** Аминь.

<>{props.isEasterOffsetRange(-3, -2) && <MdxLoader src="Matins/VelikayaEktenia" lang={props.lang} />}</>

<If condition={!props.isEasterOffsetRange(-(7*6+1))}>
    <>{(props.dayOfWeek === 0 || props.dayOfWeek === 1 || props.vospominanijaUsopshih || props.greatLent) && <MdxLoader src="Matins/Prayer1" lang={props.lang} />}</>

    <>{props.vselenskayaRoditelskayaSubbota && <MdxLoader src="Matins/Aliluja1" lang={props.lang} />}</>
    <>{props.lentSubbotas && <MdxLoader src="Matins/Aliluja2" lang={props.lang} />}</>
    <>{!props.lentSubbotas && props.greatLent && <MdxLoader src="Matins/Aliluja3" lang={props.lang} />}</>

</If>

<If condition={props.isEasterOffsetRange(-6, -4)}>
    <MdxLoader src="Shared/TroparionPassion" lang={props.lang} />
</If>
<If condition={props.isEasterOffsetRange(-3, -2)}>
    <MdxLoader src="Shared/TroparionPassion2" lang={props.lang} />
</If>
<If condition={props.vospominanijaUsopshih}>

### Тропари

{props.troparions}

  <Parts
      date={props.date}
      partNames={['shared.Богородичен']}
      serviceType="Утреня"
  />
</If>

<If condition={props.isEasterOffsetRange(-3)}>

    <span id="reading" />

    ## Чтения

    **Д** Дабы удостоиться нам слышать святое евангелие, Господа Бога молим!

    **Н** Господи, помилуй **(трижды)**.

    **П** Премудрость! Станем благоговейно! Услышим святое Евангелие!

    **П** От **(имя)** святого Благовестия чтение.

    **Н** Слава Тебе, Господи, слава Тебе!

    **П** Будем внимательны!

    <br />

    <>{props.readings}</>

    **Н** Слава Тебе, Господи, слава Тебе!

    ### Проповедь

</If>

<If condition={!props.isEasterOffsetRange(-3, -2) && !props.vselenskayaRoditelskayaSubbota}>
    <>{props.dayOfWeek === 0 || (props.dayOfWeek === 2 && <MdxLoader src="Matins/Prayer2" lang={props.lang} />)}</>
    <>{props.dayOfWeek === 0 || (props.dayOfWeek === 3 && <MdxLoader src="Matins/Prayer3" lang={props.lang} />)}</>
    <>{props.dayOfWeek === 0 || (props.dayOfWeek === 4 && <MdxLoader src="Matins/Prayer4" lang={props.lang} />)}</>
    <>{props.dayOfWeek === 0 || (props.dayOfWeek === 5 && <MdxLoader src="Matins/Prayer7" lang={props.lang} />)}</>
    <>{props.dayOfWeek === 0 || (props.dayOfWeek === 6 && <MdxLoader src="Matins/Prayer8" lang={props.lang} />)}</>
</If>

<If condition={props.vselenskayaRoditelskayaSubbota}>
    ## Кафизма 17
    <MdxLoader src="Shared/Psalm118VselSubSt1" lang={props.lang} />

    ### Малая заупокойная ектения
    <MdxLoader src="Shared/MalayaZaupokEktenya" lang={props.lang} />

    ### Молитва об усопших
    <MdxLoader src="Shared/MolitvaObUsopshihVsel" lang={props.lang} />

    <MdxLoader src="Shared/Psalm118VselSubSt2" lang={props.lang} />

    ### Малая заупокойная ектения
    <MdxLoader src="Shared/MalayaZaupokEktenya" lang={props.lang} />

    ### Молитва об усопших
    <MdxLoader src="Shared/MolitvaObUsopshihVsel" lang={props.lang} />

</If>

<MdxLoader src="Matins/Prayer10" lang={props.lang} />

<Psalm50 lang={props.lang} />

<If condition={props.isEasterOffsetRange(-3)}>
    <MdxLoader src="Matins/PassionThursdayCanon" lang={props.lang} />
</If>

<MdxLoader src="Matins/Prayer11" lang={props.lang} />

<Exapostilari date={props.date} />
<MdxLoader src="Matins/HvalitniePsalmi" lang={props.lang} lent />

### Стихиры на «Хвалите»

<Parts date={props.date} partNames={['matins.Cтихиры на хвалите']} serviceType="Утреня" />
<MdxLoader
    src="Matins/SlavoslovieLent"
    lang={props.lang}
    short={props.isEasterOffsetRange(-6, -2) || props.vospominanijaUsopshih || props.isEasterOffsetRange(-(7 * 6 + 1))}
/>

<If condition={props.isEasterOffsetRange(-6, -4) || !props.greatLent || props.dayOfWeek === 6}>

    <span id="reading" />

    ## Чтения

    <If condition={props.containsGospel}>

    **П** Премудрость! Станем благоговейно! Услышим святое евангелие!

    **П** От **(имя)** святого благовестия чтение.

    **Н** Слава Тебе, Господи, слава Тебе!

    **П** Будем внимательны!

    </If>

    <br />

    <>{props.readings}</>

    <If condition={props.containsGospel}>

    **Н** Слава Тебе, Господи, слава Тебе!

    </If>

    ### Проповедь

</If>

<>
    {(props.isEasterOffsetRange(0, 39) || props.dayOfWeek === 0) && (
        <MdxLoader src="Matins/VoskresnajaPesn" lang={props.lang} />
    )}
</>

<MdxLoader src="Shared/EkteniaOglash" lang={props.lang} />
<MdxLoader src="Matins/PrayerKatekhumens" lang={props.lang} />

**П** Оглашаемые, отступи́те!

## Заключение

**П** Завершим нашу утреннюю молитву Господу!

<MdxLoader src="Matins/Prayer12" lang={props.lang} />
<br />
<MdxLoader src="Matins/PrayerGlavopriklonnaja" lang={props.lang} />

<If condition={props.isEasterOffsetRange(-3)}>
    <MdxLoader src="Matins/PassionThursdayStihiriNaStihah" lang={props.lang} />
</If>

<If condition={props.isEasterOffsetRange(-2)}>
    <MdxLoader src="Matins/PassionFridayStihiriNaStihah" lang={props.lang} />
</If>

**Ч** Сладко Господа благодарить, имени Твоему, о Вышний, бряца́ть; возвещать поутру́ милость Твою, и верность Твою – в ночи́. **{!props.isEasterOffsetRange(-3,-2) && props.greatLent && props.dayOfWeek !== 6 && props.dayOfWeek !== 0 ? '(Дважды)' : ''}**

<MdxLoader src="Shared/STrisvatogo" lang={props.lang} />

<If condition={props.isEasterOffsetRange(-3) || !((props.sirnajaSedmitza && (props.dayOfWeek === 3 || props.dayOfWeek === 5)) || (props.greatLent && props.dayOfWeek !== 6))}>

    <If condition={!props.isEasterOffsetRange(-3)}>
        ### Тропари

        <>{props.troparions}</>
    </If>
    <If condition={props.isEasterOffsetRange(-3)}>
        <MdxLoader src="Shared/TroparionPassion2" lang={props.lang} />
    </If>

    <Parts
        date={props.date}
        partNames={['shared.Богородичен']}
        serviceType="Утреня"
    />

    <MdxLoader src="Shared/SugubajaKratkaja" lang={props.lang} />

    <>{props.otpust}</>

</If>

<If condition={!props.isEasterOffsetRange(-3) && ((props.sirnajaSedmitza && (props.dayOfWeek === 3 || props.dayOfWeek === 5)) || (props.greatLent && props.dayOfWeek !== 6))}>

    <If condition={!props.greatLent}>
        <MdxLoader src="Matins/LentEnding1" lang={props.lang} />
    </If>
    <If condition={props.greatLent}>
        <MdxLoader src="Matins/LentEnding2" lang={props.lang} />
    </If>

    ### Отпуст

    <Parts
        date={props.date}
        partNames={['shared.Отпуст Синаксарный']}
        saints={props.saints}
        fallback={
            <MdxLoader src="Shared/Ending/Otpust" />
        }
        serviceType="Утреня"
    />

    <>{props.saints}</>

    <MdxLoader src="Shared/Ending/Tverdina" isEasterOffsetRange={props.isEasterOffsetRange} lang={props.lang} isFast={props.isFast} />

</If>

<>{props.endHymns}</>
