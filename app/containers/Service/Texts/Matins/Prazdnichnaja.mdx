import MdxLoader from 'containers/Service/Texts/MdxLoader';
import Prokimens from 'containers/Service/Texts/Matins/Prokimens/Prokimens';
import Stepenny from 'containers/Service/Texts/Matins/Stepenny/Stepenny';
import Exapostilari from 'containers/Service/Texts/Matins/Exapostilari/Exapostilari';
import Parts from 'components/Parts/Parts';
import If from 'components/If/If';
import StihiriNaHvalite from './StihiriNaHvalite/StihiriNaHvalite';
import Psalm50 from 'containers/Service/Texts/Shared/Psalm50/index.mdx';

<span id="nachalo" />

# Утреня

<br />

**П** Слава святой, и единосущной, и животворящей, и нераздельной Троице ныне и всегда и во веки веков!

**Н** Аминь.

{props.easterSeason && (props.brightOrOtdanie ? <MdxLoader src="Shared/DaVosstanetBog" /> : <MdxLoader src="Shared/EasterTroparion3x" />)}

<>{(props.isHoliday || props.brightWeek) && <MdxLoader src="Matins/VelikayaEktenia" lang={props.lang} />}</>

<>{(props.isHoliday || props.dayOfWeek === 1) && <MdxLoader src="Matins/Prayer1" lang={props.lang} />}</>

<>{props.isHoliday && <MdxLoader src="Matins/BogGospod" lang={props.lang} />}</>

<If condition={props.isHoliday}>
  {props.troparions}
  <Parts
      date={props.date}
      partNames={['shared.Богородичен синаксарный']}
  />
</If>

<>{!props.brightWeek && (props.isHoliday || props.dayOfWeek === 2) && <MdxLoader src="Matins/Prayer2" lang={props.lang} />}</>
<>{!props.brightWeek && (props.isHoliday || props.dayOfWeek === 3) && <MdxLoader src="Matins/Prayer3" lang={props.lang} />}</>
<>{!props.brightWeek && (props.isHoliday || props.dayOfWeek === 4) && <MdxLoader src="Matins/Prayer4" lang={props.lang} />}</>
<>{!props.brightWeek && (props.isHoliday || props.dayOfWeek === 5) && <MdxLoader src="Matins/Prayer7" lang={props.lang} />}</>

<>{props.isHoliday && <MdxLoader src="Matins/Polijeley" lang={props.lang} />}</>
<>{(props.isEasterOffsetRange(-9 * 7) || props.isEasterOffsetRange(-8 * 7) || props.isEasterOffsetRange(-7 * 7)) && <MdxLoader src="Matins/Psalm136" lang={props.lang} />}</>

<>{props.isHoliday && <Parts date={props.date} partNames={['shared.Величания']} Layout={props.SectionLayout} />}</>

<>{(props.dayOfWeek === 0 && !props.isEasterOffsetRange(-7) && !props.isEasterOffsetRange(49)) && <MdxLoader src="Matins/TropsVoskr" lang={props.lang} />}</>

<>{(props.isHoliday || props.dayOfWeek === 6) && <MdxLoader src="Matins/Prayer8" lang={props.lang} />}</>

<>{props.isHoliday && <MdxLoader src="Matins/Prayer10" lang={props.lang} />}</>
<>{props.isHoliday && <Psalm50 lang={props.lang} />}</>
<>{props.dayOfWeek === 0 && !props.isEasterOffsetRange(49) && <MdxLoader src="Matins/PesnBogoroditsy" lang={props.lang} />}</>

<MdxLoader src="Matins/Prayer11" lang={props.lang} />

<>{props.dayOfWeek === 0 && !props.isEasterOffsetRange(49) && <MdxLoader src="Matins/Sviat" lang={props.lang} />}</>
<Exapostilari date={props.date}/>

<MdxLoader src="Matins/HvalitniePsalmi" lang={props.lang} />

### Стихиры на «Хвалите»

<StihiriNaHvalite date={props.date} day={props.day} />
{!props.brightWeek && <MdxLoader src="Matins/Slavoslovie" lang={props.lang} />}

<If condition={!props.brightWeek && props.dayOfWeek !== 0 || props.isEasterOffsetRange(-7)}>
  ### Тропари

  {props.troparions}
  <Parts
    date={props.date}
    partNames={['shared.Богородичен синаксарный']}
  />
</If>

{props.brightWeek && <MdxLoader src="Shared/EasterTroparion3x" />}

<>{props.dayOfWeek === 0 && !props.isEasterOffsetRange(-7) && !props.isEasterOffsetRange(49) && <MdxLoader src="Matins/TropsOtp" lang={props.lang} glas={props.day.glas} />}</>

<span id="reading" />

## Чтения

<Stepenny day={props.day} date={props.date} />

<Prokimens day={props.day} date={props.date} />

<>{props.isHoliday && <MdxLoader src="Matins/Prayer9" lang={props.lang} />}</>

<>{props.isHoliday && !props.isEasterOffsetRange(49) && <MdxLoader src="Matins/Prokimen2" lang={props.lang} />}</>

<>{props.isHoliday && <MdxLoader src="Matins/Vstavka" lang={props.lang} />}</>

**П** Премудрость! Станем благоговейно! Услышим святое евангелие!

**П** От **(имя)** святого благовестия чтение.

**Н** Слава Тебе, Господи, слава Тебе!

**П** Будем внимательны!

<br />

<>{props.readings}</>

**Н** Слава Тебе, Господи, слава Тебе!

### Проповедь

<>{!props.isEasterOffsetRange(-7) && !props.isEasterOffsetRange(49) && (props.isEasterOffsetRange(-1 * 7 - 1) || props.isEasterOffsetRange(0, 39) || props.dayOfWeek === 0) && <MdxLoader src="Matins/VoskresnajaPesn" lang={props.lang} x3={props.brightWeek || (props.easterSeason && props.dayOfWeek === 0)} />}</>

<MdxLoader src="Shared/EkteniaOglash" lang={props.lang} />
<MdxLoader src="Matins/PrayerKatekhumens" lang={props.lang} />

**П** Оглашаемые, отступи́те!

<MdxLoader src="Shared/SugubajaKratkaja" lang={props.lang} />

## Заключение

**П** Завершим нашу утреннюю молитву Господу!

<MdxLoader src="Matins/Prayer12" lang={props.lang} />
<br />
<MdxLoader src="Matins/PrayerGlavopriklonnaja" lang={props.lang} />

<>{props.otpust}</>

<>{props.endHymns}</>
