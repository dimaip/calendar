import MdxLoader from 'containers/Service/Texts/MdxLoader';
import Html from 'components/Html/Html';
import Parts from 'components/Parts/Parts';
import StihiriNaGV from './StihiriNaGV/StihiriNaGV';
import StihiriNaStihah from './StihiriNaStihah/StihiriNaStihah';
import Prokimens from 'containers/Service/Texts/Vespers/Prokimens/Prokimens';
import If from 'components/If/If';

<span id="nachalo" />

# Вечерня

<br />

**П** Благословен Бог наш ныне и всегда и во веки веков!

**Н** Аминь.

{props.easterSeason && (props.brightOrOtdanie ? <MdxLoader src="Shared/DaVosstanetBog" /> : <MdxLoader src="Shared/EasterTroparion3x" />)}

<>{props.isHoliday && !props.easterSeason && <MdxLoader src="Vespers/Priidite" lang={props.lang} />}</>

<>{props.isHoliday && <MdxLoader src="Vespers/Psalm103" lang={props.lang} />}</>

<>{(props.isHoliday || props.brightWeek) && <MdxLoader src="Vespers/VelikajaEktenia" lang={props.lang} />}</>

<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 1) && (
        <MdxLoader src="Vespers/Prayer1" lang={props.lang} />
    )}
</>
<>{props.isHoliday && <MdxLoader src="Vespers/Kafisma" lang={props.lang} />}</>
<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 2) && (
        <MdxLoader src="Vespers/Prayer2" lang={props.lang} />
    )}
</>
<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 3) && (
        <MdxLoader src="Vespers/Prayer3" lang={props.lang} />
    )}
</>
<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 4) && (
        <MdxLoader src="Vespers/Prayer4" lang={props.lang} />
    )}
</>
<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 5) && (
        <MdxLoader src="Vespers/Prayer5" lang={props.lang} />
    )}
</>
<>
    {!props.brightWeek && !props.isAscension && props.isHoliday && (
        <MdxLoader src="Vespers/Prayer6" lang={props.lang} />
    )}
</>
<>
    {!props.brightWeek && !props.isAscension && (props.isHoliday || props.dayOfWeek === 0) && (
        <MdxLoader src="Vespers/Prayer7" lang={props.lang} />
    )}
</>

<MdxLoader src="Vespers/Prayer8" lang={props.lang} />
<MdxLoader src="Vespers/VecherniePsalmi" lang={props.lang} />

### Стихиры на «Господи, взываю»

<StihiriNaGV day={props.day} date={props.date} />
<span id="vhod" />

## Вход

**П** Премудрость! Станем благоговейно!

### Вечерняя (входная) песнь «при возжжении светильника»

<MdxLoader src="Vespers/JasniSvet" lang={props.lang} />

## Чтения

<Prokimens date={props.date} />
<span id="reading" />

**П** Премудрость!

**Ч** Из книги… чтение.

**П** Будем внимательны!

<br />

<>{props.readings}</>

### Проповедь

<MdxLoader src="Shared/EkteniaOglash" lang={props.lang} />
<MdxLoader src="Vespers/PrayerKatekhumens" lang={props.lang} />

**П** Оглашаемые, отступи́те!

<MdxLoader src="Shared/SugubajaPolnaja" lang={props.lang} />
<MdxLoader src="Vespers/Udostoi" lang={props.lang} />

## Заключение

**П** Завершим нашу вечернюю молитву Господу!

<MdxLoader src="Vespers/Prayer9" lang={props.lang} />
<br />
<MdxLoader src="Vespers/PrayerGlavopriklonnaja" lang={props.lang} />

### Стихиры «на стихах»

<StihiriNaStihah day={props.day} date={props.date} />

<If condition={!props.brightWeek}>
    <MdxLoader src="Vespers/PrayerSimeona" lang={props.lang} />
    <MdxLoader src="Shared/STrisvatogo" lang={props.lang} />

    ### Тропари

    <>{props.troparions}</>
    <Parts
        date={props.date}
        partNames={['shared.Богородичен синаксарный']}
        serviceType="Вечерня"
    />

</If>
<If condition={props.brightWeek}>
    <MdxLoader src="Shared/EasterTroparion3x" />
</If>

<>{props.otpust}</>

<>{props.endHymns}</>
