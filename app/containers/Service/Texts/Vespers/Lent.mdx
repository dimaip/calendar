import MdxLoader from 'containers/Service/Texts/MdxLoader';
import Parts from 'components/Parts/Parts';
import Html from 'components/Html/Html';

import Prokimens, { DefatulProkimen } from 'containers/Service/Texts/Vespers/Prokimens/Prokimens';
import StihiriNaGV from './StihiriNaGV/StihiriNaGV';
import StihiriNaStihah from './StihiriNaStihah/StihiriNaStihah';

<span id="nachalo" />

# Вечерня

<br />

**П** Благословен Бог наш ныне и всегда и во веки веков!

<If condition={props.annunciation}>
    <MdxLoader src="Shared/ObichnoeNachalo" lang={props.lang} />
    <MdxLoader src="Vespers/Psalm103" lang={props.lang} chitaetsa />
    <MdxLoader src="Vespers/VelikajaEktenia" lang={props.lang} />
</If>

<If condition={!props.sirnajaSedmitza && !props.greatLent && !props.annunciation && !props.troitskayaRoditelskayaSubbota}>

    **Ч** Аминь. Слава Тебе, Боже наш, слава Тебе!

    <MdxLoader src="Shared/Paraklit" lang={props.lang} />

</If>

<If condition={props.sirnajaSedmitza || props.greatLent}>**{props.chinProshenija ? 'Ч' : 'Н'}** Аминь.</If>

<If condition={props.chinProshenija}>
    <MdxLoader src="Shared/Pridite3" lang={props.lang}  />

    ### Псалом 103

    <MdxLoader src="Shared/Psalm103" lang={props.lang}  />
    <MdxLoader src="Vespers/VelikajaEktenia" lang={props.lang} />

</If>

<>
    {(props.isAnnunciation || props.dayOfWeek === 6 || props.dayOfWeek === 1) && (
        <MdxLoader src="Vespers/Prayer1" lang={props.lang} />
    )}
</>
<>
    {!props.isAnnunciation && (props.dayOfWeek === 6 || props.dayOfWeek === 2) && (
        <MdxLoader src="Vespers/Prayer2" lang={props.lang} />
    )}
</>
<>
    {!props.isAnnunciation && (props.dayOfWeek === 6 || props.dayOfWeek === 3) && (
        <MdxLoader src="Vespers/Prayer3" lang={props.lang} />
    )}
</>
<>
    {!props.isAnnunciation && (props.dayOfWeek === 6 || props.dayOfWeek === 4) && (
        <MdxLoader src="Vespers/Prayer4" lang={props.lang} />
    )}
</>
<>
    {!props.isAnnunciation && (props.dayOfWeek === 6 || props.dayOfWeek === 5) && (
        <MdxLoader src="Vespers/Prayer5" lang={props.lang} />
    )}
</>
<>{!props.isAnnunciation && props.dayOfWeek === 6 && <MdxLoader src="Vespers/Prayer6" lang={props.lang} />}</>
<>
    {!props.isAnnunciation && !props.greatLent && (props.dayOfWeek === 6 || props.dayOfWeek === 0) && (
        <MdxLoader src="Vespers/Prayer7" lang={props.lang} />
    )}
</>

<MdxLoader src="Vespers/Prayer8" lang={props.lang} />

<If condition={props.greatLent && props.dayOfWeek !== 5 && !props.isEasterOffsetRange(-(7 * 7 - 1))}>
    <Then>
        <MdxLoader src="Vespers/VecherniePsalmiLent" lang={props.lang} />
    </Then>
    <Else>
        <MdxLoader src="Vespers/VecherniePsalmi" lang={props.lang} />
    </Else>
</If>

### Стихиры на «Господи, взываю»

<StihiriNaGV day={props.day} date={props.date} />
<span id="vhod" />

<If condition={props.dayOfWeek === 0 && props.greatLent || props.annunciation}>

## Вход

**П** Премудрость! Станем благоговейно!

</If>

### Вечерняя (входная) песнь «при возжжении светильника»

<MdxLoader src="Vespers/JasniSvet" lang={props.lang} />

## Чтения

{props.annunciation ? <DefatulProkimen date={props.date}/> : <Prokimens date={props.date} />}

<span id="reading" />

**П** Премудрость!

**Ч** Из книги… чтение.

**П** Будем внимательны!

<br />

<>{props.readings}</>

### Проповедь

<MdxLoader src="Shared/EkteniaOglash" lang={props.lang} />
<MdxLoader src="Vespers/PrayerKatekhumens" lang={props.lang} />

**П** Оглашаемые, отступи́те!

<MdxLoader src="Vespers/Udostoi" lang={props.lang} lent />

## Заключение

**П** Завершим нашу вечернюю молитву Господу!

<MdxLoader src="Vespers/Prayer9" lang={props.lang} />
<br />
<MdxLoader src="Vespers/PrayerGlavopriklonnaja" lang={props.lang} />

### Стихиры «на стихах»

<StihiriNaStihah day={props.day} date={props.date} />
<MdxLoader src="Vespers/PrayerSimeona" lang={props.lang} lent />
<MdxLoader src="Shared/STrisvatogo" lang={props.lang} />

<If condition={!((props.greatLent && props.dayOfWeek !== 5) || props.sirnajaSedmitza && (props.dayOfWeek === 2 || props.dayOfWeek === 4))}>

    ### Тропари

    <>{props.troparions}</>

    <Parts date={props.date} partNames={['shared.Богородичен']} serviceType="Вечерня" />

    <MdxLoader src="Shared/SugubajaKratkaja" lang={props.lang} />

    <>{props.otpust}</>

</If>

<If condition={(props.sirnajaSedmitza && (props.dayOfWeek === 2 || props.dayOfWeek === 4))}>

    <MdxLoader src="Vespers/LentEnding1" lang={props.lang} />

    ### Отпуст

    <Parts
        date={props.date}
        partNames={['shared.Отпуст Синаксарный']}
        fallback={
            props.isSunday || props.easterSeason ? (
                <MdxLoader src="Shared/Ending/EasterOtpust" />
            ) : (
                <MdxLoader src="Shared/Ending/Otpust" />
            )
        }
        serviceType="Вечерня"
    />

    <>{props.saints}</>

    <MdxLoader src="Shared/Ending/Tverdina" isEasterOffsetRange={props.isEasterOffsetRange} lang={props.lang} isFast={props.isFast} />

</If>

<If condition={(props.greatLent && props.dayOfWeek !== 5)}>

    <If condition={!props.isEasterOffsetRange(-5, -3) && props.dayOfWeek === 0}>
        <MdxLoader src="Vespers/LentEnding2" lang={props.lang} />
    </If>
    <If condition={!props.isEasterOffsetRange(-5, -3) && props.dayOfWeek !== 0}>
        <MdxLoader src="Vespers/LentEnding3" lang={props.lang} />
    </If>
    <If condition={props.isEasterOffsetRange(-5, -3)}>
        <MdxLoader src="Shared/TroparionPassion" lang={props.lang} />
        <MdxLoader src="Vespers/LentEnding5" lang={props.lang} />
    </If>

    ### Отпуст

    <Parts
        date={props.isEasterOffsetRange(-3, -2) ? props.previousDate : props.date}
        partNames={['shared.Отпуст Синаксарный']}
        fallback={<MdxLoader src="Shared/Ending/Otpust" />}
        serviceType="Вечерня"
    />

    <>{props.saints}</>

    <If condition={props.chinProshenija}>
        <Then>
            ### Чин прощения с пением 136 псалма

            <MdxLoader src="Matins/Psalm136" lang={props.lang} />
        </Then>
        <Else>
            <MdxLoader src="Shared/Ending/Tverdina" isEasterOffsetRange={props.isEasterOffsetRange} lang={props.lang} isFast={props.isFast} />
        </Else>
    </If>

</If>

<>{props.endHymns}</>
