import Tooltip from 'components/Tooltip/Tooltip';
import Html from 'components/Html/Html';
import If from 'components/If/If';
import MdxLoader from 'containers/Service/Texts/MdxLoader';
import Parts from 'components/Parts/Parts';

<div id="trisvatoe" />

### Молитва перед пением <br />Трисвятой песни<Tooltip>Если Трисвятое не поётся, то даётся только возглас.</Tooltip>

<If condition={!props.vmestoTrisvatogoExists}>
    <MdxLoader src="Liturgies/Katekhumen/Trisvatoe/MolitvaPered" />

    > **[Тихо:**

    > **Д** Благослови, владыка, пение Трисвято́го! **]**
</If>

**После благословения выходит дьякон перед свя­ты­ми вра­тами и, поводя орарём, даёт тон сто­ящему народу, го­воря:**

**Д** и во веки веков.

**Н** Аминь.

<If condition={!props.vmestoTrisvatogoExists}>
    ### Трисвятое

    <MdxLoader src="Liturgies/Katekhumen/Trisvatoe/Trisvatoe" />
</If>

<Parts
    date={props.date}
    partNames={['liturgy.Вместо Трисвятого']}
    Layout={({ children }) => (
        <>
  ### Вместо Трисвятого

            {children}
        </>
    )}
/>
